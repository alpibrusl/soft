# soft-agent

Autonomous agent runtime for the soft project.

> **Status:** Skeleton. Implementation awaits the four lex-lang issues this crate depends on. See [`../../docs/crates/soft-agent.md`](../../docs/crates/soft-agent.md) for scope.

## Build

```
cargo build -p soft-agent
```

(Builds clean as an empty crate today; non-trivial code lands as upstream lex-lang issues #184–#187 ship.)
